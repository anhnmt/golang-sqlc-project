version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    database:
      managed: true
    gen:
      go:
        package: "postgresql"
        out: "postgresql"
        sql_package: "pgx/v5"